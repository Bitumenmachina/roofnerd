{
  "name": "roofnerd",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test && pnpm test:shell",
    "dev": "tauri dev",
    "gates": "bash tools/gate-language.sh && bash tools/gate-egress.sh && bash tools/gate-clientdata.sh --all",
    "build:app": "NO_STRIP=true tauri build",
    "test:shell": "cargo test --quiet --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4",
    "puppeteer-core": "^25.10.0"
  }
}
