{
  "name": "roofnerd",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "tauri dev",
    "gates": "bash tools/gate-language.sh && bash tools/gate-egress.sh",
    "build:app": "NO_STRIP=true tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4",
    "puppeteer-core": "^25.10.0"
  }
}
