{
  "name": "@roofnerd/app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "test": "echo \"the app has no tests of its own — the arithmetic is tested in @roofnerd/engine\""
  },
  "dependencies": {
    "@roofnerd/engine": "workspace:*",
    "@tauri-apps/api": "2.9.0"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vite": "8.2.2"
  }
}
